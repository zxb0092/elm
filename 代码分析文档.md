# 项目代码分析文档

## 项目概述

**项目名称**：elm_Spring（外卖平台后端服务）  
**技术栈**：Spring Boot 3.4.0 · Java 21 · MyBatis Plus 3.5.9 · MySQL · FastJSON2 · JJWT · Lombok  
**架构层次**：Controller → Service → Mapper → 数据库

---

## 目录结构

```
src/main/java/org/example/elm_spring/
├── ElmSpringApplication.java          # 启动类
├── config/
│   ├── AccessControlAllowOriginFilter.java  # 跨域配置
│   └── WebConfig.java                       # MVC 配置（拦截器注册）
├── controller/                        # 控制器层（接收 HTTP 请求）
│   ├── BusinessController.java
│   ├── CartController.java
│   ├── DeliveryAddressController.java
│   ├── FoodController.java
│   ├── OrdersController.java
│   └── UserController.java
├── interceptor/
│   └── LoginInterceptor.java          # 登录鉴权拦截器
├── mapper/                            # 数据访问层（MyBatis 接口）
│   ├── BusinessMapper.java
│   ├── CartMapper.java
│   ├── DeliveryAddressMapper.java
│   ├── FoodMapper.java
│   ├── OrdersMapper.java
│   └── UserMapper.java
├── pojo/                              # 实体类（数据模型）
│   ├── Business.java
│   ├── Cart.java
│   ├── DeliveryAddress.java
│   ├── Food.java
│   ├── OrderDetailet.java
│   ├── Orders.java
│   ├── Result.java
│   └── User.java
├── service/                           # 业务逻辑接口层
│   ├── BusinessService.java
│   ├── CartService.java
│   ├── DeliveryAddressService.java
│   ├── FoodService.java
│   ├── OrdersService.java
│   └── UserService.java
├── service/impl/                      # 业务逻辑实现层
│   ├── BusinessServiceImpl.java
│   ├── CartServiceImpl.java
│   ├── DeliveryAddressImpl.java
│   ├── FoodServiceImpl.java
│   ├── OrdersServiceImpl.java
│   └── UserServiceImpl.java
└── utils/
    └── JwtUtils.java                  # JWT 工具类
```

---

## 一、启动类

### `ElmSpringApplication`

**作用**：Spring Boot 应用入口，启动整个 Web 服务。

| 方法 | 签名 | 作用 |
|------|------|------|
| `main` | `public static void main(String[] args)` | 调用 `SpringApplication.run()` 启动 Spring Boot 应用 |

---

## 二、配置类（config）

### `AccessControlAllowOriginFilter`

**作用**：全局跨域配置类，允许前端页面跨域访问所有接口。实现了 `WebMvcConfigurer` 接口。

> ⚠️ **注意**：当前配置允许所有来源（`allowedOrigins("*")`）、所有请求头和所有 HTTP 方法，便于开发调试，但在生产环境中应将 `allowedOrigins` 限制为实际前端域名，以避免跨站请求伪造等安全风险。

| 方法 | 签名 | 作用 |
|------|------|------|
| `addCorsMappings` | `public void addCorsMappings(CorsRegistry registry)` | 注册跨域规则，对所有路径（`/*/**`）开放全部请求头、全部 HTTP 方法，允许所有来源，预检缓存时间 1800 秒 |

---

### `WebConfig`

**作用**：Web MVC 配置类，注册登录拦截器并设置白名单（不需要登录的接口）。实现了 `WebMvcConfigurer` 接口。

| 方法 | 签名 | 作用 |
|------|------|------|
| `addInterceptors` | `public void addInterceptors(InterceptorRegistry registry)` | 将 `LoginInterceptor` 注册到拦截器链，并排除以下无需登录的路径：`/login`、`/register`、`/business`、`/businessList`（白名单中保留，但当前无对应控制器映射）、`/business/orderTypeId/{orderTypeId}`、`/business/search/{name}`、`/business/businessId/{businessId}`、`/food/{businessId}/{userId}`、`/food/businessId/{businessId}` |

---

## 三、拦截器（interceptor）

### `LoginInterceptor`

**作用**：JWT 登录验证拦截器。对所有需要登录的接口，在请求到达控制器之前校验请求头中的 Token，未携带或 Token 非法则直接返回错误响应，阻止请求继续。实现了 `HandlerInterceptor` 接口。

| 方法 | 签名 | 作用 |
|------|------|------|
| `preHandle` | `public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception` | 从请求头 `Authorization` 中获取 Token；若为空则向响应写入"没有登录"错误并返回 `false`；否则调用 `JwtUtils.parseToken()` 解析，解析成功返回 `true`（放行），解析失败返回 `false`（拦截） |

---

## 四、实体类（pojo）

### `Business`（商家）

**作用**：封装商家信息，同时承载一对多关联的食物列表和购物车列表，用于多层数据聚合返回。

| 字段 | 类型 | 含义 |
|------|------|------|
| `businessId` | `Integer` | 商家 ID（主键） |
| `businessName` | `String` | 商家名称 |
| `businessAddress` | `String` | 商家地址 |
| `businessExplain` | `String` | 商家简介 |
| `businessImg` | `String` | 商家图片 URL |
| `orderTypeId` | `Integer` | 订单分类 ID |
| `starPrice` | `double` | 起送价 |
| `deliveryPrice` | `double` | 配送费 |
| `remarks` | `String` | 备注 |
| `foodList` | `List<Food>` | 关联食物列表（一对多） |
| `cartList` | `List<Cart>` | 关联购物车列表（一对多） |

> 该类使用 Lombok `@Data`、`@NoArgsConstructor`、`@AllArgsConstructor` 自动生成 getter/setter、无参构造、全参构造。

---

### `Cart`（购物车）

**作用**：封装购物车条目信息，记录某用户在某商家中对某食物的加购数量，并可关联食物详情。

| 字段 | 类型 | 含义 |
|------|------|------|
| `cartId` | `Integer` | 购物车条目 ID（主键） |
| `foodId` | `Integer` | 食物 ID |
| `businessId` | `Integer` | 商家 ID |
| `userId` | `Long` | 用户 ID |
| `quantity` | `Integer` | 数量 |
| `food` | `Food` | 关联的食物对象 |

---

### `DeliveryAddress`（收货地址）

**作用**：封装用户收货地址信息。

| 字段 | 类型 | 含义 |
|------|------|------|
| `daId` | `Integer` | 地址 ID（主键） |
| `contactName` | `String` | 联系人姓名 |
| `contactSex` | `Integer` | 联系人性别 |
| `contactTel` | `String` | 联系人电话 |
| `address` | `String` | 详细地址 |
| `userId` | `Long` | 所属用户 ID |
| `user` | `User` | 关联用户对象 |

---

### `Food`（食物）

**作用**：封装食物信息，并可关联所属商家及对应购物车条目（用于返回食物的加购状态）。

| 字段 | 类型 | 含义 |
|------|------|------|
| `foodId` | `Integer` | 食物 ID（主键） |
| `foodName` | `String` | 食物名称 |
| `foodExplain` | `String` | 食物简介 |
| `foodImg` | `String` | 食物图片 URL |
| `foodPrice` | `Double` | 食物单价 |
| `businessId` | `Integer` | 所属商家 ID |
| `remarks` | `String` | 备注 |
| `business` | `Business` | 关联商家对象 |
| `cart` | `Cart` | 关联购物车条目（用于返回当前用户的加购数量和 cartId） |

---

### `OrderDetailet`（订单明细）

**作用**：封装订单中每一条食物明细记录，并可关联食物详情（价格、图片、名称等）。

| 字段 | 类型 | 含义 |
|------|------|------|
| `odId` | `Integer` | 明细 ID（主键） |
| `orderId` | `Integer` | 所属订单 ID |
| `foodId` | `Integer` | 食物 ID |
| `quantity` | `Integer` | 购买数量 |
| `food` | `Food` | 关联食物对象 |

---

### `Orders`（订单）

**作用**：封装订单主记录，并聚合地址、订单明细列表、购物车列表、商家等关联数据。

| 字段 | 类型 | 含义 |
|------|------|------|
| `orderId` | `Integer` | 订单 ID（主键） |
| `userId` | `Long` | 用户 ID |
| `businessId` | `Integer` | 商家 ID |
| `orderDate` | `String` | 下单时间 |
| `orderTotal` | `Double` | 订单总价 |
| `daId` | `Integer` | 收货地址 ID |
| `orderState` | `Integer` | 订单状态（0=未支付，1=已支付） |
| `deliveryAddress` | `DeliveryAddress` | 关联收货地址 |
| `orderDetailetList` | `List<OrderDetailet>` | 关联订单明细列表 |
| `cartList` | `List<Cart>` | 关联购物车列表 |
| `business` | `Business` | 关联商家对象 |

---

### `Result`（统一响应结果）

**作用**：统一 API 响应体，封装响应码、描述信息和返回数据，供所有 Controller 使用。

| 字段 | 类型 | 含义 |
|------|------|------|
| `code` | `Integer` | 响应码（1=成功，0=失败） |
| `msg` | `String` | 描述信息 |
| `data` | `Object` | 返回数据 |

| 方法 | 签名 | 作用 |
|------|------|------|
| `success()` | `public static Result success()` | 创建表示增删改成功的响应（data 为 null） |
| `success(data)` | `public static Result success(Object data)` | 创建表示查询成功的响应，并携带返回数据 |
| `error(msg)` | `public static Result error(String msg)` | 创建表示失败的响应，并携带错误描述信息 |

---

### `User`（用户）

**作用**：封装用户信息，登录成功后额外存储 JWT Token，并可关联收货地址列表。

| 字段 | 类型 | 含义 |
|------|------|------|
| `userId` | `Long` | 用户 ID（主键） |
| `password` | `String` | 密码 |
| `userName` | `String` | 用户名 |
| `userSex` | `Integer` | 性别 |
| `userImg` | `String` | 头像 URL |
| `delTag` | `Integer` | 删除标记（1=正常） |
| `token` | `String` | JWT Token（登录后赋值，不存库） |
| `deliveryAddressList` | `List<DeliveryAddress>` | 关联收货地址列表 |

---

## 五、数据访问层（mapper）

### `BusinessMapper`

**作用**：操作 `business`（商家）及辅助操作 `food`、`cart` 表的 MyBatis Mapper 接口，提供商家的增删查以及购物车辅助查询。

| 方法 | 签名 | SQL 操作 | 作用 |
|------|------|----------|------|
| `list` | `List<Business> list()` | `SELECT * FROM business` | 查询所有商家 |
| `search` | `List<Business> search(String name)` | `SELECT * FROM business WHERE businessName LIKE '%name%'` | 按商家名模糊搜索 |
| `getByOrderTypeId` | `List<Business> getByOrderTypeId(Integer orderTypeId)` | `SELECT * FROM business WHERE orderTypeId = ?` | 按订单类型查询商家列表 |
| `getByBusinessId` | `Business getByBusinessId(Integer businessId)` | `SELECT * FROM business WHERE businessId = ?` | 按主键查询单个商家 |
| `add` | `void add(Business business)` | `INSERT INTO business(...)` | 新增商家 |
| `deleteBusiness` | `void deleteBusiness(Integer businessId)` | `DELETE FROM business WHERE businessId = ?` | 删除商家记录 |
| `deleteFood` | `void deleteFood(Integer businessId)` | `DELETE FROM food WHERE businessId = ?` | 删除该商家下所有食物（商家删除联动） |
| `getCartByBusiness` | `List<Cart> getCartByBusiness(Integer businessId, Long userId)` | `SELECT * FROM cart WHERE businessId = ? AND userId = ? AND quantity != 0` | 查询某用户在某商家中所有数量不为零的购物车条目 |
| `getPrice` | `Double getPrice(Integer foodId)` | `SELECT foodPrice FROM food WHERE foodId = ?` | 查询指定食物的单价 |

---

### `CartMapper`

**作用**：操作 `cart`（购物车）表的 MyBatis Mapper 接口，提供购物车的增、改、查、清空。

| 方法 | 签名 | SQL 操作 | 作用 |
|------|------|----------|------|
| `addCart` | `void addCart(Cart cart)` | `INSERT INTO cart(foodId, businessId, userId, quantity)` | 新增购物车条目 |
| `addQuantity` | `void addQuantity(Integer cartId)` | `UPDATE cart SET quantity = quantity + 1 WHERE cartId = ?` | 将指定购物车条目数量加 1 |
| `substrateQuantity` | `void substrateQuantity(Integer cartId)` | `UPDATE cart SET quantity = quantity - 1 WHERE cartId = ?` | 将指定购物车条目数量减 1 |
| `getCartQuantity` | `Integer getCartQuantity(Integer cartId)` | `SELECT quantity FROM cart WHERE cartId = ?` | 查询指定购物车条目的当前数量 |
| `setQuantity` | `void setQuantity(Integer cartId)` | `UPDATE cart SET quantity = 0 WHERE cartId = ?` | 将指定购物车条目数量强制置零 |
| `deleteAllCart` | `void deleteAllCart(Long userId)` | `DELETE FROM cart WHERE userId = ?` | 删除指定用户的全部购物车记录 |

---

### `DeliveryAddressMapper`

**作用**：操作 `deliveryaddress`（收货地址）表的 MyBatis Mapper 接口，提供地址的增删改查。

| 方法 | 签名 | SQL 操作 | 作用 |
|------|------|----------|------|
| `list` | `List<DeliveryAddress> list(Long userId)` | `SELECT * FROM deliveryaddress WHERE userId = ?` | 查询指定用户的所有收货地址 |
| `addAddress` | `void addAddress(DeliveryAddress deliveryAddress)` | `INSERT INTO deliveryaddress(...)` | 新增收货地址 |
| `deleteAddress` | `void deleteAddress(Integer daId)` | `DELETE FROM deliveryaddress WHERE daId = ?` | 删除指定收货地址 |
| `updateAddress` | `void updateAddress(DeliveryAddress deliveryAddress)` | `UPDATE deliveryaddress SET ... WHERE daId = ?` | 修改收货地址（联系人、性别、电话、地址） |
| `getByDaId` | `DeliveryAddress getByDaId(Integer daId)` | `SELECT * FROM deliveryaddress WHERE daId = ?` | 按主键查询单条收货地址 |

---

### `FoodMapper`

**作用**：操作 `food`（食物）及辅助查询 `cart` 表的 MyBatis Mapper 接口。

| 方法 | 签名 | SQL 操作 | 作用 |
|------|------|----------|------|
| `listFoodByBusiness` | `List<Food> listFoodByBusiness(Integer businessId)` | `SELECT * FROM food WHERE businessId = ?` | 查询某商家下的所有食物（不携带用户购物车信息） |
| `listFood` | `List<Food> listFood(Integer businessId, Long userId)` | `SELECT * FROM food WHERE businessId = ?` | 查询某商家下的所有食物（供服务层注入当前用户的购物车信息） |
| `addFood` | `void addFood(Food food)` | `INSERT INTO food(...)` | 新增食物 |
| `getCard` | `Cart getCard(Integer foodId, Long userId)` | `SELECT * FROM cart WHERE foodId = ? AND userId = ?` | 查询指定用户对指定食物的购物车条目 |
| `getQuantity` | `Integer getQuantity(Integer foodId, Long userId)` | `SELECT quantity FROM cart WHERE foodId = ? AND userId = ?` | 查询指定用户对指定食物的加购数量 |
| `getCartId` | `Integer getCartId(Integer foodId, Long userId)` | `SELECT cartId FROM cart WHERE foodId = ? AND userId = ?` | 查询指定用户对指定食物的购物车条目 ID |
| `deleteFood` | `void deleteFood(Integer foodId)` | `DELETE FROM food WHERE foodId = ?` | 删除指定食物 |

---

### `OrdersMapper`

**作用**：操作 `orders`（订单）、`orderdetailet`（订单明细）、及辅助查询 `cart`、`food`、`business`、`deliveryaddress` 表的 MyBatis Mapper 接口。

| 方法 | 签名 | SQL 操作 | 作用 |
|------|------|----------|------|
| `addOrders` | `void addOrders(Orders orders)` | `INSERT INTO orders(...)` | 插入订单主记录 |
| `getCarts` | `List<Cart> getCarts(Integer businessId, Long userId)` | `SELECT * FROM cart WHERE businessId = ? AND userId = ? AND quantity != 0` | 查询用户在某商家下所有有效购物车条目（用于生成订单明细） |
| `addOrderDetailet` | `void addOrderDetailet(OrderDetailet orderDetailet)` | `INSERT INTO orderdetailet(...)` | 插入单条订单明细记录 |
| `updateCartQuantity` | `void updateCartQuantity(Integer cartId)` | `UPDATE cart SET quantity = 0 WHERE cartId = ?` | 将已生成订单的购物车条目数量清零 |
| `getOrderDetailets` | `List<OrderDetailet> getOrderDetailets(Integer orderId)` | `SELECT * FROM orderdetailet WHERE orderId = ?` | 查询指定订单的所有明细 |
| `check` | `Integer check(Orders orders)` | `SELECT COUNT(*) FROM orders WHERE userId = ? AND businessId = ? AND orderState = 0` | 检查该用户在该商家是否已有未支付的订单 |
| `getAddressByDaId` | `DeliveryAddress getAddressByDaId(Integer daId)` | `SELECT * FROM deliveryaddress WHERE daId = ?` | 按地址 ID 查询收货地址 |
| `getFoodByFoodId` | `Food getFoodByFoodId(Integer foodId)` | `SELECT * FROM food WHERE foodId = ?` | 按食物 ID 查询食物信息 |
| `getBusinessId` | `Integer getBusinessId(Integer orderId)` | `SELECT businessId FROM orders WHERE orderId = ?` | 通过订单 ID 查询所属商家 ID |
| `getBusiness` | `Business getBusiness(Integer businessId)` | `SELECT * FROM business WHERE businessId = ?` | 按商家 ID 查询商家信息 |
| `getOrderByOrderId` | `Orders getOrderByOrderId(Integer orderId)` | `SELECT * FROM orders WHERE orderId = ?` | 按订单 ID 查询订单主记录 |
| `updateOrders` | `void updateOrders(Orders orders)` | `UPDATE orders SET orderState = ? WHERE orderId = ?` | 更新订单状态 |
| `deleteOrders` | `void deleteOrders(Integer orderId)` | `DELETE FROM orders WHERE orderId = ?` | 删除订单主记录 |
| `deleteOrderDetails` | `void deleteOrderDetails(Integer orderId)` | `DELETE FROM orderdetailet WHERE orderId = ?` | 删除订单的所有明细（随订单联动删除） |

---

### `UserMapper`

**作用**：操作 `user`（用户）及辅助查询 `orders`、`orderdetailet`、`food`、`business` 表的 MyBatis Mapper 接口。

| 方法 | 签名 | SQL 操作 | 作用 |
|------|------|----------|------|
| `getUserInfo` | `User getUserInfo(Long userId)` | `SELECT * FROM user WHERE userId = ?` | 按用户 ID 查询用户信息 |
| `login` | `User login(User user)` | `SELECT * FROM user WHERE userName = ? AND password = ?` | 验证用户名和密码，返回用户对象（用于登录） |
| `register` | `void register(User user)` | `INSERT INTO user(...)` | 注册新用户 |
| `getOrdersNotPay` | `List<Orders> getOrdersNotPay(Long userId)` | `SELECT * FROM orders WHERE userId = ? AND orderState = 0` | 查询用户所有未支付的订单 |
| `getOrdersPay` | `List<Orders> getOrdersPay(Long userId)` | `SELECT * FROM orders WHERE userId = ? AND orderState = 1` | 查询用户所有已支付的订单 |
| `getBusiness` | `Business getBusiness(Integer businessId)` | `SELECT * FROM business WHERE businessId = ?` | 按商家 ID 查询商家信息（订单列表聚合用） |
| `getOrderDetailet` | `List<OrderDetailet> getOrderDetailet(Integer orderId)` | `SELECT * FROM orderdetailet WHERE orderId = ?` | 查询指定订单的所有明细（订单列表聚合用） |
| `getFood` | `Food getFood(Integer foodId)` | `SELECT * FROM food WHERE foodId = ?` | 按食物 ID 查询食物信息（订单明细聚合用） |
| `getUserByUserName` | `User getUserByUserName(String userName)` | `SELECT * FROM user WHERE userName = ?` | 按用户名查询用户（用于注册前的重名校验） |

---

## 六、业务逻辑接口层（service）

### `BusinessService`

**作用**：定义商家相关的业务逻辑接口规范。

| 方法 | 签名 | 作用 |
|------|------|------|
| `list` | `List<Business> list()` | 获取所有商家列表 |
| `search` | `List<Business> search(String name)` | 按名称模糊搜索商家 |
| `getByOrderTypeId` | `List<Business> getByOrderTypeId(Integer orderTypeId)` | 按订单类型筛选商家 |
| `add` | `void add(Business business)` | 新增商家 |
| `delete` | `void delete(Integer businessId)` | 删除商家（含其下所有食物） |
| `getByBusinessId` | `Business getByBusinessId(Integer businessId)` | 按 ID 查询单个商家 |
| `total` | `Double total(Integer businessId, Long userId)` | 计算用户在某商家购物车内食物的总价（含配送费） |
| `getQuantity` | `Integer getQuantity(Integer businessId, Long userId)` | 统计用户在某商家购物车内食物的总件数 |

---

### `CartService`

**作用**：定义购物车相关的业务逻辑接口规范。

| 方法 | 签名 | 作用 |
|------|------|------|
| `addCart` | `void addCart(Cart cart)` | 向购物车新增一条食物条目 |
| `addQuantity` | `void addQuantity(Integer cartId)` | 购物车指定条目数量加 1 |
| `substrateQuantity` | `void substrateQuantity(Integer cartId)` | 购物车指定条目数量减 1（不低于零） |
| `deleteAllCart` | `void deleteAllCart(Long userId)` | 清空指定用户的所有购物车记录 |

---

### `DeliveryAddressService`

**作用**：定义收货地址相关的业务逻辑接口规范。

| 方法 | 签名 | 作用 |
|------|------|------|
| `list` | `List<DeliveryAddress> list(Long userId)` | 获取指定用户的所有收货地址 |
| `addAddress` | `void addAddress(DeliveryAddress deliveryAddress)` | 新增收货地址 |
| `deleteAddress` | `void deleteAddress(Integer daId)` | 删除指定收货地址 |
| `updateAddress` | `void updateAddress(DeliveryAddress deliveryAddress)` | 修改收货地址 |
| `getByDaId` | `DeliveryAddress getByDaId(Integer daId)` | 按 ID 查询单个收货地址 |

---

### `FoodService`

**作用**：定义食物相关的业务逻辑接口规范。

| 方法 | 签名 | 作用 |
|------|------|------|
| `listFoodByBusinessId` | `List<Food> listFoodByBusinessId(Integer businessId)` | 查询某商家的食物列表（不携带用户购物车状态，数量默认为 0） |
| `listFood` | `List<Food> listFood(Integer businessId, Long userId)` | 查询某商家的食物列表并注入当前用户的购物车信息（数量、cartId） |
| `getCart` | `Cart getCart(Integer foodId, Long userId)` | 查询指定用户对指定食物的购物车条目 |
| `addFood` | `void addFood(Food food)` | 新增食物 |
| `deleteFood` | `void deleteFood(Integer foodId)` | 删除指定食物 |

---

### `OrdersService`

**作用**：定义订单相关的业务逻辑接口规范。

| 方法 | 签名 | 作用 |
|------|------|------|
| `addOrders` | `Integer addOrders(Orders orders)` | 创建订单，成功返回订单 ID，若该商家已有未支付订单则返回 -1 |
| `getOrderDetailets` | `List<OrderDetailet> getOrderDetailets(Integer orderId)` | 查询指定订单的所有明细（含食物详情） |
| `getBusinessByOrderId` | `Business getBusinessByOrderId(Integer orderId)` | 通过订单 ID 获取所属商家信息 |
| `getOrderByOrderId` | `Orders getOrderByOrderId(Integer orderId)` | 按订单 ID 查询订单（含收货地址） |
| `updateOrders` | `void updateOrders(Orders orders)` | 更新订单状态 |
| `deleteOrders` | `void deleteOrders(Integer orderId)` | 删除订单及其所有明细 |

---

### `UserService`

**作用**：定义用户相关的业务逻辑接口规范。

| 方法 | 签名 | 作用 |
|------|------|------|
| `getUserInfo` | `User getUserInfo(Long userId)` | 查询用户基本信息 |
| `login` | `User login(User user)` | 用户登录验证 |
| `register` | `User register(User user)` | 用户注册（含重名校验） |
| `getOrdersNotPay` | `List<Orders> getOrdersNotPay(Long userId)` | 查询用户所有未支付订单（聚合商家和明细信息） |
| `getOrdersPay` | `List<Orders> getOrdersPay(Long userId)` | 查询用户所有已支付订单（聚合商家和明细信息） |

---

## 七、业务逻辑实现层（service/impl）

### `BusinessServiceImpl`

**作用**：`BusinessService` 的实现类，实现商家相关的具体业务逻辑，注入 `BusinessMapper`。

| 方法 | 签名 | 作用 |
|------|------|------|
| `list` | `public List<Business> list()` | 委托 Mapper 查询所有商家 |
| `search` | `public List<Business> search(String name)` | 委托 Mapper 按名称模糊搜索 |
| `getByOrderTypeId` | `public List<Business> getByOrderTypeId(Integer orderTypeId)` | 委托 Mapper 按订单类型过滤商家 |
| `add` | `public void add(Business business)` | 委托 Mapper 新增商家 |
| `delete` | `public void delete(Integer businessId)` | 先删商家记录，再删该商家下的所有食物（两步联动） |
| `getByBusinessId` | `public Business getByBusinessId(Integer businessId)` | 委托 Mapper 按 ID 查询商家 |
| `total` | `public Double total(Integer businessId, Long userId)` | 遍历购物车中所有有效条目，累计 `foodPrice × quantity`，再加上该商家的配送费，返回总价 |
| `getQuantity` | `public Integer getQuantity(Integer businessId, Long userId)` | 遍历购物车中所有有效条目，累计各条目的数量，返回总件数 |

---

### `CartServiceImpl`

**作用**：`CartService` 的实现类，实现购物车相关的具体业务逻辑，注入 `CartMapper`。

| 方法 | 签名 | 作用 |
|------|------|------|
| `addCart` | `public void addCart(Cart cart)` | 将新加入的购物车条目数量初始化为 1，再调用 Mapper 插入 |
| `addQuantity` | `public void addQuantity(Integer cartId)` | 直接委托 Mapper 将数量加 1 |
| `substrateQuantity` | `public void substrateQuantity(Integer cartId)` | 先查询当前数量：若大于 0 则减 1；若已为 0 则强制置零（防止负数） |
| `deleteAllCart` | `public void deleteAllCart(Long userId)` | 委托 Mapper 删除该用户所有购物车记录 |

---

### `DeliveryAddressImpl`

**作用**：`DeliveryAddressService` 的实现类，实现收货地址相关的具体业务逻辑，注入 `DeliveryAddressMapper`。所有方法均为简单委托（pass-through），无额外业务逻辑。

| 方法 | 签名 | 作用 |
|------|------|------|
| `list` | `public List<DeliveryAddress> list(Long userId)` | 委托 Mapper 查询用户所有地址 |
| `addAddress` | `public void addAddress(DeliveryAddress deliveryAddress)` | 委托 Mapper 新增地址 |
| `deleteAddress` | `public void deleteAddress(Integer daId)` | 委托 Mapper 删除指定地址 |
| `updateAddress` | `public void updateAddress(DeliveryAddress deliveryAddress)` | 委托 Mapper 更新地址信息 |
| `getByDaId` | `public DeliveryAddress getByDaId(Integer daId)` | 委托 Mapper 按 ID 查询地址 |

---

### `FoodServiceImpl`

**作用**：`FoodService` 的实现类，实现食物相关的具体业务逻辑，注入 `FoodMapper`。核心逻辑是在返回食物列表时，为每个食物注入当前用户的购物车状态。

| 方法 | 签名 | 作用 |
|------|------|------|
| `listFoodByBusinessId` | `public List<Food> listFoodByBusinessId(Integer businessId)` | 查询商家食物列表，为每个食物创建一个 `quantity=0` 的空购物车对象挂载（适用于未登录用户浏览） |
| `listFood` | `public List<Food> listFood(Integer businessId, Long userId)` | 查询商家食物列表，为每个食物从 `cart` 表查询当前用户的实际加购数量、cartId 等信息并注入到 `food.cart` 中（适用于已登录用户浏览） |
| `getCart` | `public Cart getCart(Integer foodId, Long userId)` | 委托 Mapper 查询指定用户对指定食物的购物车条目 |
| `addFood` | `public void addFood(Food food)` | 委托 Mapper 新增食物 |
| `deleteFood` | `public void deleteFood(Integer foodId)` | 委托 Mapper 删除食物 |

---

### `OrdersServiceImpl`

**作用**：`OrdersService` 的实现类，实现订单相关的核心业务逻辑，注入 `OrdersMapper` 和 `CartMapper`。

| 方法 | 签名 | 作用 |
|------|------|------|
| `addOrders` | `public Integer addOrders(Orders orders)` | 创建订单的完整流程：①检查该用户在该商家是否有未支付订单，有则返回 -1；②无则生成随机订单 ID、记录当前时间、设置状态为未支付，插入订单主记录；③从购物车查出所有有效条目，逐条生成订单明细并插入；④将这些购物车条目数量清零；⑤清空该用户全部购物车；⑥返回新订单 ID |
| `getOrderDetailets` | `public List<OrderDetailet> getOrderDetailets(Integer orderId)` | 查询订单明细列表，并为每条明细从数据库查询对应食物的名称、价格、图片、简介等信息并填入 `orderDetailet.food` |
| `getBusinessByOrderId` | `public Business getBusinessByOrderId(Integer orderId)` | 先通过订单 ID 查到商家 ID，再查商家信息返回 |
| `getOrderByOrderId` | `public Orders getOrderByOrderId(Integer orderId)` | 查询订单主记录，并附带查询对应的收货地址信息一并返回 |
| `updateOrders` | `public void updateOrders(Orders orders)` | 委托 Mapper 更新订单状态（如从未支付改为已支付） |
| `deleteOrders` | `public void deleteOrders(Integer orderId)` | 先删除订单的所有明细，再删除订单主记录（两步联动） |

---

### `UserServiceImpl`

**作用**：`UserService` 的实现类，实现用户相关的具体业务逻辑，注入 `UserMapper`。

| 方法 | 签名 | 作用 |
|------|------|------|
| `getUserInfo` | `public User getUserInfo(Long userId)` | 委托 Mapper 按 ID 查询用户基本信息 |
| `login` | `public User login(User user)` | 委托 Mapper 按用户名和密码查询，若找到则返回用户对象，否则返回 null |
| `register` | `public User register(User user)` | 先按用户名查找是否已存在：已存在则返回 null（注册失败）；不存在则设置 `delTag=1`、用 UUID 生成 userId，调用 Mapper 写库并返回新用户对象 |
| `getOrdersNotPay` | `public List<Orders> getOrdersNotPay(Long userId)` | 查询用户所有未支付订单，并为每个订单填充商家信息、订单明细列表（每条明细再填充食物信息） |
| `getOrdersPay` | `public List<Orders> getOrdersPay(Long userId)` | 查询用户所有已支付订单，并为每个订单填充商家信息、订单明细列表（每条明细再填充食物信息） |

---

## 八、控制器层（controller）

### `BusinessController`

**作用**：处理商家相关的 HTTP 请求，提供商家的 CRUD 及购物车汇总接口，注入 `BusinessService`。

| 方法 | HTTP 方法 & 路径 | 签名 | 作用 |
|------|-----------------|------|------|
| `list` | `GET /business` | `public Result list()` | 获取所有商家列表 |
| `search` | `GET /business/search/{name}` | `public Result search(@PathVariable String name)` | 按名称模糊搜索商家 |
| `getByOrderTypeId` | `GET /business/orderTypeId/{orderTypeId}` | `public Result getByOrderTypeId(@PathVariable Integer orderTypeId)` | 按订单类型筛选商家 |
| `getByBusinessId` | `GET /business/businessId/{businessId}` | `public Result getByBusinessId(@PathVariable Integer businessId)` | 按 ID 查询单个商家详情 |
| `add` | `POST /business` | `public Result add(@RequestBody Business business)` | 新增商家 |
| `delete` | `DELETE /business/{businessId}` | `public Result delete(@PathVariable Integer businessId)` | 删除商家（含其下所有食物） |
| `total` | `GET /business/total/{businessId}/{userId}` | `public Result total(@PathVariable Integer businessId, @PathVariable Long userId)` | 查询用户在某商家购物车内商品的总价（含配送费） |
| `quantity` | `GET /business/quantity/{businessId}/{userId}` | `public Result quantity(@PathVariable Integer businessId, @PathVariable Long userId)` | 查询用户在某商家购物车内商品的总件数 |

---

### `CartController`

**作用**：处理购物车相关的 HTTP 请求，注入 `CartService`。

| 方法 | HTTP 方法 & 路径 | 签名 | 作用 |
|------|-----------------|------|------|
| `addCart` | `POST /cart` | `public Result addCart(@RequestBody Cart cart)` | 向购物车添加一条新条目（数量初始为 1） |
| `addQuantity` | `PUT /cart/addQuantity/{cartId}` | `public Result addQuantity(@PathVariable Integer cartId)` | 将指定购物车条目数量加 1 |
| `substrateQuantity` | `PUT /cart/substrateQuantity/{cartId}` | `public Result substrateQuantity(@PathVariable Integer cartId)` | 将指定购物车条目数量减 1（防止负数） |

---

### `DeliveryAddressController`

**作用**：处理收货地址相关的 HTTP 请求，注入 `DeliveryAddressService`。

| 方法 | HTTP 方法 & 路径 | 签名 | 作用 |
|------|-----------------|------|------|
| `list` | `GET /addressList/{userId}` | `public Result list(@PathVariable Long userId)` | 获取指定用户的所有收货地址 |
| `addAddress` | `POST /addressList` | `public Result addAddress(@RequestBody DeliveryAddress deliveryAddress)` | 新增收货地址 |
| `deleteAddress` | `DELETE /addressList/{daId}` | `public Result deleteAddress(@PathVariable Integer daId)` | 删除指定收货地址 |
| `updateAddress` | `PUT /addressList` | `public Result updateAddress(@RequestBody DeliveryAddress deliveryAddress)` | 修改收货地址信息 |
| `getByDaId` | `GET /addressList/daId/{daId}` | `public Result getByDaId(@PathVariable Integer daId)` | 按 ID 查询单条收货地址 |

---

### `FoodController`

**作用**：处理食物相关的 HTTP 请求，注入 `FoodService`。

| 方法 | HTTP 方法 & 路径 | 签名 | 作用 |
|------|-----------------|------|------|
| `listFoodByBusinessId` | `GET /food/businessId/{businessId}` | `public Result listFoodByBusinessId(@PathVariable Integer businessId)` | 查询某商家的食物列表（不含用户购物车状态，适用于未登录） |
| `listFood` | `GET /food/{businessId}/{userId}` | `public Result listFood(@PathVariable Integer businessId, @PathVariable Long userId)` | 查询某商家食物列表并附带用户购物车状态（已登录） |
| `getCart` | `GET /food/getCart/{foodId}/{userId}` | `public Result getCart(@PathVariable Integer foodId, @PathVariable Long userId)` | 查询指定用户对指定食物的购物车条目 |
| `addFood` | `POST /food` | `public Result addFood(@RequestBody Food food)` | 新增食物 |
| `deleteFood` | `DELETE /food/{foodId}` | `public Result deleteFood(@PathVariable Integer foodId)` | 删除指定食物 |

---

### `OrdersController`

**作用**：处理订单相关的 HTTP 请求，注入 `OrdersService`。

| 方法 | HTTP 方法 & 路径 | 签名 | 作用 |
|------|-----------------|------|------|
| `addOrders` | `POST /orders` | `public Result addOrders(@RequestBody Orders orders)` | 创建订单；若该商家已有未支付订单则返回错误提示，否则返回新订单 ID |
| `getOrderDetailets` | `GET /orderDetailet/{orderId}` | `public Result getOrderDetailets(@PathVariable Integer orderId)` | 查询指定订单的所有明细（含食物信息） |
| `getBusinessByOrderId` | `GET /order/businessInfo/{orderId}` | `public Result getBusinessByOrderId(@PathVariable Integer orderId)` | 通过订单 ID 获取所属商家信息 |
| `getOrderByOrderId` | `GET /orders/{orderId}` | `public Result getOrderByOrderId(@PathVariable Integer orderId)` | 按 ID 查询订单详情（含收货地址） |
| `updateOrders` | `PUT /orders` | `public Result updateOrders(@RequestBody Orders orders)` | 更新订单状态（如支付） |
| `deleteOrders` | `DELETE /orders/{orderId}` | `public Result deleteOrders(@PathVariable Integer orderId)` | 删除订单及其所有明细 |

---

### `UserController`

**作用**：处理用户相关的 HTTP 请求，注入 `UserService`；登录时使用 `JwtUtils` 生成 Token。

| 方法 | HTTP 方法 & 路径 | 签名 | 作用 |
|------|-----------------|------|------|
| `getUserInfo` | `GET /userInfo/{userId}` | `public Result getUserInfo(@PathVariable Long userId)` | 查询用户基本信息 |
| `login` | `POST /login` | `public Result login(@RequestBody User user)` | 用户登录：验证用户名和密码，成功则生成 JWT Token（含 userId、userName，有效期 30 分钟）并返回用户对象；失败则返回错误提示 |
| `register` | `POST /register` | `public Result register(@RequestBody User user)` | 用户注册：若用户名已存在返回错误，否则完成注册并返回新用户对象 |
| `getOrdersByUserIdNotPay` | `GET /orderListNotPay/{userId}` | `public Result getOrdersByUserIdNotPay(@PathVariable Long userId)` | 查询用户所有未支付订单（聚合商家和明细信息） |
| `getOrdersByUserIdPay` | `GET /orderListPay/{userId}` | `public Result getOrdersByUserIdPay(@PathVariable Long userId)` | 查询用户所有已支付订单（聚合商家和明细信息） |

---

## 九、工具类（utils）

### `JwtUtils`

**作用**：JWT（JSON Web Token）工具类，负责 Token 的生成与解析。通过 PBKDF2WithHmacSHA512 算法对短密钥 `"zxb"` 进行扩展，以满足 HS256 签名的密钥长度要求，提升安全性。

| 方法 / 构造器 | 签名 | 作用 |
|--------------|------|------|
| `JwtUtils()` | 构造器 | 在实例化时使用 PBKDF2WithHmacSHA512 算法（迭代 10000 次，输出 512 位）对原始密钥进行扩展，将结果 Base64 编码后存入 `extendedKey` 字段 |
| `createToken` | `public String createToken(Map<String, Object> map)` | 根据传入的载荷 Map（如 userId、userName）生成 JWT Token，签发时间为当前时间，有效期 **30 分钟**（硬编码于源码 `System.currentTimeMillis() + 30 * 60 * 1000`，不可通过配置文件调整），使用 HS256 算法签名，返回 Token 字符串 |
| `parseToken` | `public Claims parseToken(String token)` | 使用扩展密钥解析并验证 JWT Token，返回 Claims（载荷）；Token 非法或过期时抛出异常 |

---

## 十、接口汇总

| 分类 | 接口路径 | HTTP 方法 | 是否需要登录 | 说明 |
|------|---------|-----------|------------|------|
| 用户 | `/login` | POST | ❌ | 用户登录 |
| 用户 | `/register` | POST | ❌ | 用户注册 |
| 用户 | `/userInfo/{userId}` | GET | ✅ | 查询用户信息 |
| 用户 | `/orderListNotPay/{userId}` | GET | ✅ | 查询未支付订单 |
| 用户 | `/orderListPay/{userId}` | GET | ✅ | 查询已支付订单 |
| 商家 | `/business` | GET | ❌ | 查询所有商家 |

| 商家 | `/business/search/{name}` | GET | ❌ | 搜索商家 |
| 商家 | `/business/orderTypeId/{orderTypeId}` | GET | ❌ | 按类型筛选商家 |
| 商家 | `/business/businessId/{businessId}` | GET | ❌ | 按 ID 查询商家 |
| 商家 | `/business` | POST | ✅ | 新增商家 |
| 商家 | `/business/{businessId}` | DELETE | ✅ | 删除商家 |
| 商家 | `/business/total/{businessId}/{userId}` | GET | ✅ | 购物车总价 |
| 商家 | `/business/quantity/{businessId}/{userId}` | GET | ✅ | 购物车总件数 |
| 食物 | `/food/businessId/{businessId}` | GET | ❌ | 查询商家食物（无购物车状态） |
| 食物 | `/food/{businessId}/{userId}` | GET | ❌ | 查询商家食物（含购物车状态） |
| 食物 | `/food/getCart/{foodId}/{userId}` | GET | ✅ | 查询食物购物车条目 |
| 食物 | `/food` | POST | ✅ | 新增食物 |
| 食物 | `/food/{foodId}` | DELETE | ✅ | 删除食物 |
| 购物车 | `/cart` | POST | ✅ | 加入购物车 |
| 购物车 | `/cart/addQuantity/{cartId}` | PUT | ✅ | 数量加 1 |
| 购物车 | `/cart/substrateQuantity/{cartId}` | PUT | ✅ | 数量减 1 |
| 地址 | `/addressList/{userId}` | GET | ✅ | 查询用户地址列表 |
| 地址 | `/addressList` | POST | ✅ | 新增地址 |
| 地址 | `/addressList/{daId}` | DELETE | ✅ | 删除地址 |
| 地址 | `/addressList` | PUT | ✅ | 修改地址 |
| 地址 | `/addressList/daId/{daId}` | GET | ✅ | 按 ID 查询地址 |
| 订单 | `/orders` | POST | ✅ | 创建订单 |
| 订单 | `/orderDetailet/{orderId}` | GET | ✅ | 查询订单明细 |
| 订单 | `/order/businessInfo/{orderId}` | GET | ✅ | 订单关联商家信息 |
| 订单 | `/orders/{orderId}` | GET | ✅ | 按 ID 查询订单 |
| 订单 | `/orders` | PUT | ✅ | 更新订单状态 |
| 订单 | `/orders/{orderId}` | DELETE | ✅ | 删除订单 |
